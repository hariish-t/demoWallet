name: Android Build
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout code
      - uses: actions/checkout@v3
      
      # 2️⃣ Setup JDK
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
          
      # 3️⃣ Setup Node
      - uses: actions/setup-node@v3
        with:
          node-version: 20
          
      # 4️⃣ Install Yarn
      - run: npm install -g yarn
      
      # 5️⃣ Remove node_modules to ensure fresh install
      - name: Remove node_modules
        run: rm -rf node_modules
        
      # 6️⃣ Clean Gradle cache
      - name: Clean Gradle cache
        run: rm -rf ~/.gradle/caches/
          
      # 7️⃣ Install dependencies
      - run: yarn install --frozen-lockfile
      
      # 8️⃣ Set NODE_BINARY for Gradle
      - name: Set NODE_BINARY
        run: echo "NODE_BINARY=$(which node)" >> $GITHUB_ENV
        
      # 9️⃣ Clean Gradle build outputs
      - name: Clean Gradle
        run: cd android && ./gradlew clean && cd ..
        
      # 🔟 Bundle React Native assets
      - name: Bundle React Native assets
        run: |
          mkdir -p android/app/src/main/assets
          npx react-native bundle \
            --platform android \
            --dev false \
            --entry-file index.js \
            --bundle-output android/app/src/main/assets/index.android.bundle \
            --assets-dest android/app/src/main/res \
            --reset-cache
            
      # 1️⃣1️⃣ Setup Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        
      # 1️⃣2️⃣ Install Android SDK & NDK
      - name: Install Android SDK & NDK
        run: |
          yes | sdkmanager \
            "platform-tools" \
            "platforms;android-34" \
            "build-tools;34.0.0" \
            "ndk;27.1.12297006"
            
      # 1️⃣3️⃣ Accept licenses
      - run: yes | sdkmanager --licenses

      # Disable New Architecture to avoid C++ compilation issues
      - name: Disable New Architecture
        run: |
          echo "newArchEnabled=false" >> android/gradle.properties
          echo "hermesEnabled=true" >> android/gradle.properties
      
      # 1️⃣4️⃣ Build APK with warnings enabled to see deprecated usage
      - name: Build APK
        run: cd android && ./gradlew assembleRelease --no-daemon --stacktrace --warning-mode all

      # 1️⃣5️⃣ Upload artifact
      - uses: actions/upload-artifact@v4
        with:
          name: demoWallet-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
