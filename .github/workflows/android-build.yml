name: Android Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - uses: actions/checkout@v3

      # 2️⃣ Setup JDK 17
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      # 3️⃣ Setup Node.js 20
      - uses: actions/setup-node@v3
        with:
          node-version: 20

      # 4️⃣ Install Yarn globally
      - run: npm install -g yarn

      # 5️⃣ Cache node_modules & Gradle
      - name: Cache Node modules & Gradle
        uses: actions/cache@v3
        with:
          path: |
            node_modules
            android/.gradle
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}

      # 6️⃣ Install dependencies
      - run: yarn install --frozen-lockfile

      # 7️⃣ Ensure react-native-reanimated plugin exists
      - name: Ensure reanimated plugin
        run: |
          if [ ! -d "node_modules/react-native-reanimated" ]; then
            yarn add react-native-reanimated
          fi

      # 8️⃣ Patch reanimated for CI
      - name: Patch react-native-reanimated
        run: |
          export NODE_BINARY=$(which node)
          if [ -f ./node_modules/react-native-reanimated/scripts/prepare.js ]; then
            node ./node_modules/react-native-reanimated/scripts/prepare.js
          else
            echo "Reanimated prepare.js not found, skipping..."
          fi

      # 9️⃣ Pre-bundle React Native assets
      - name: Bundle React Native assets
        run: |
          mkdir -p android/app/src/main/assets
          npx react-native bundle \
            --platform android \
            --dev false \
            --entry-file index.js \
            --bundle-output android/app/src/main/assets/index.android.bundle \
            --assets-dest android/app/src/main/res \
            --reset-cache

      # 🔟 Setup Android SDK
      - name: Install Android SDK & NDK
        run: |
          yes | sdkmanager --licenses
          yes | sdkmanager \
            "platform-tools" \
            "platforms;android-34" \
            "build-tools;34.0.0" \
            "ndk;27.1.12297006"

      # 1️⃣1️⃣ Clean Gradle
      - name: Clean Gradle
        run: cd android && ./gradlew clean && cd ..

      # 1️⃣2️⃣ Build Release APK
      - name: Build APK
        run: cd android && ./gradlew assembleRelease --no-daemon --stacktrace

      # 1️⃣3️⃣ Upload APK artifact
      - uses: actions/upload-artifact@v4
        with:
          name: demoWallet-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
